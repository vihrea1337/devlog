# Нормализуем переводы строк: в репозитории — LF.
* text=auto eol=lf
# Windows-скрипты — CRLF.
*.bat text eol=crlf
# gradlew — Unix-скрипт, всегда LF (иначе не запустится на Linux при CI/деплое).
gradlew text eol=lf
# Бинарники не трогать.
*.jar binary
