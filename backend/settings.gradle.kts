// Имя Gradle-проекта бэкенда. Самостоятельный проект внутри репозитория DevLog.
rootProject.name = "devlog-backend"

// Общий модуль с моделями обмена (DTO): один набор классов на бэкенд и клиентов.
// includeBuild подключает соседний Gradle-проект как обычную зависимость.
includeBuild("../shared")
