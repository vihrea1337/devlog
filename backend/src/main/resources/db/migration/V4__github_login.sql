-- V4 — логин GitHub у пользователя (для импорта коммитов как сырья для дневника).
--
-- Токен НЕ храним: на старте берём только публичную активность через открытый API,
-- поэтому секретов здесь нет и защищать нечего. Приватные репозитории потребуют токена —
-- тогда появится отдельная таблица integrations с ссылкой на секрет, а не сам секрет.
ALTER TABLE users ADD COLUMN IF NOT EXISTS github_login VARCHAR(39) NULL;
