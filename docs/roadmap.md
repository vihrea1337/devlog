# DevLog — дорожная карта

Принцип: **MVP-first**. Каждая фаза заканчивается чем-то реально работающим и полезным,
даже если бросить проект на середине. Архитектура с первого дня многопользовательская
(под SaaS/портфолио), но строим постепенно.

## Фаза 0 — Проектирование ← СЕЙЧАС
- [x] Концепция продукта (`product-concept.md`)
- [x] Архитектура + черновик контракта API (`architecture.md`)
- [x] Схема БД (`data-model.md`)
- [x] Дорожная карта (`roadmap.md`)
- [x] `CLAUDE.md` — контекст репозитория
- [x] Каркас Ktor: собирается, `GET /health` → `{"status":"ok"}` («шагающий скелет»), тест зелёный
- [x] git init + первый коммит (локально)
- [ ] Пользователь просмотрел и утвердил документы
- [ ] GitHub-репозиторий (по желанию: `gh repo create vihrea1337/devlog --private --source=backend/.. --push`)

## Фаза 1 — MVP, веб, с аккаунтами
Сердцевина продукта. Сценарий целиком в браузере.
- [x] Таблицы БД на Exposed (`Tables.kt`) + подключение (`Database.kt`) + `docker-compose.yml` — компилируется, фэт-джар собирается. _Живая проверка (создание таблиц в БД) — после `docker compose up` (Docker был выключен)._
- [x] JWT-авторизация: регистрация, вход, `GET /api/me` (bcrypt-хеш пароля, JWT-токен). Покрыто интеграционными тестами на H2 (4 теста) — зелёные.
- [x] CRUD записей (`/api/entries`, per-user, изоляция пользователей) — 5 тестов на H2 зелёные
- [x] Фоновая обработка ИИ — заготовка на **Groq** (OpenAI-совместимый; `AiProcessor` + `EntryStructuredRepository`, строгий JSON, статусы queued→processing→structured/failed). Юнит-тесты разбора + интеграционный на H2 зелёные. **Осталось: вставить ключ Groq** в `backend/secrets.properties` (`groq.api.key`)
- [ ] Генерация отчёта за период (`/api/reports`) + экспорт в Markdown
- [x] Веб-страница от бэкенда (`resources/static/index.html`): вход/регистрация, форма записи, лента по дням со статусами и структурой ИИ, авто-обновление пока идёт обработка. _Кнопка «отчёт за месяц» — после эндпоинта отчётов._
- [ ] Лимиты ИИ на пользователя
- **Готово, когда:** можно зарегистрироваться, добавить заметку, получить структуру
  и скачать отчёт за месяц — в браузере.

## Фаза 2 — Качество отчётов
- [ ] Экспорт отчёта в PDF
- [ ] Шаринг отчёта по ссылке (`/r/{shareToken}`, без авторизации)
- [ ] Несколько проектов, фильтры ленты
- [ ] Юнит- и интеграционные тесты + CI (GitHub Actions)
- [ ] Деплой на сервер брата (Docker + Caddy + DuckDNS)

## Фаза 3 — Первый мобильный клиент (Android)
- [ ] Общий KMP-модуль: модели + Ktor Client + SQLDelight
- [ ] Android-приложение на Compose Multiplatform
- [ ] Офлайн-кэш и синхронизация (updated_at, last-write-wins)

## Фаза 4 — Киллер-фичи и SaaS-контур
- [ ] Импорт коммитов GitHub/GitLab как сырья
- [ ] Импорт из чатов / выгрузок Claude Code
- [ ] Роли/команды (RBAC), если понадобится
- [ ] Тарифы, лимиты, биллинг (если делаем коммерческий продукт)

## Фаза 5 — Остальные платформы из того же кода
- [ ] iOS из готового KMP-модуля (UI почти общий с Android)
- [ ] Desktop (Windows/Mac/Linux)
- [ ] «Настоящий» веб на Compose/Wasm или отдельном фронтенде
- [ ] Полировка, мониторинг, публикация в сторы
